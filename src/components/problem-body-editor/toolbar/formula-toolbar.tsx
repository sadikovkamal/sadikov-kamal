"use client";

/**
 * STUB — Phase 3 will implement the formula toolbar.
 * Renders template buttons (fraction, root, power, sum, …) that insert
 * formula templates into the TipTap editor at the current cursor position.
 */

export interface FormulaToolbarProps {
  onInsert: (latexTemplate: string) => void;
}

export function FormulaToolbar(_props: FormulaToolbarProps) {
  return null;
}
