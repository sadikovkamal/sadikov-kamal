/**
 * STUB — Phase 3 will populate formula template definitions.
 * Each template is a LaTeX string with MathLive placeholder syntax
 * (e.g. \frac{#0}{#1}) that the toolbar inserts at the cursor.
 */

export interface FormulaTemplate {
  label: string;
  latex: string;
}

/** Placeholder — will be filled in Phase 3. */
export const FORMULA_TEMPLATES: FormulaTemplate[] = [];
