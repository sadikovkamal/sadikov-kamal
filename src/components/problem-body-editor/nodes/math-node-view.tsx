"use client";

/**
 * STUB — Phase 2 will implement the shared math node-view.
 * Resting state: KaTeX render. Focused state: MathLive <math-field>.
 * Fallback: raw-LaTeX text input when MathLive cannot parse the formula.
 */

export {};
