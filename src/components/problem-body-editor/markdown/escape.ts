/**
 * STUB — Phase 1 will implement Markdown text-escaping helpers.
 * Needed to prevent plain-text paragraph content from being parsed as
 * Markdown syntax when serialised back to body_md.
 */

/** Escapes Markdown special characters in a plain-text string. */
export function escapeMarkdownText(_text: string): string {
  throw new Error("escapeMarkdownText: not yet implemented (Phase 1)");
}
